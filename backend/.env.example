# backend/.env.example

# --- Database Configuration (Required) ---
# Your MySQL database host (e.g., localhost, or a remote IP)
DB_HOST="your_database_host"
# The name of your database
DB_NAME="your_database_name"
# The username to access the database
DB_USER="your_database_user"
# The password for the database user
DB_PASS="your_database_password"
# The port for the database connection (3306 is the default for MySQL)
DB_PORT="3306"

# --- Telegram Bot Configuration (Required) ---
# The token for your Telegram Bot, obtained from BotFather
TELEGRAM_BOT_TOKEN="your_telegram_bot_token"
# A secret string used to secure your webhook. Generate a long, random string.
TELEGRAM_WEBHOOK_SECRET="your_secret_webhook_token"

# --- Cloudflare Worker Configuration (Required) ---
# A secret string shared between the email worker and the backend to authenticate requests.
# This should be different from the Telegram secret.
EMAIL_HANDLER_SECRET="your_worker_secret_token"

# --- Application Configuration (Optional but Recommended) ---
# The public URL of your backend application.
# This is used by scripts like set_webhook.php to register the correct webhook URL.
BACKEND_URL="https://your_backend_domain.com"
