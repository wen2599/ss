# Backend Configuration
# Copy this file to .env and fill in the values.

# -- Database Settings --
# Your database host (e.g., 127.0.0.1 for local)
DB_HOST=127.0.0.1

# Your database name
DB_NAME=lottery_app

# Your database username
DB_USER=root

# Your database password (leave empty if none)
DB_PASS=

# -- Security Settings --
# A secret key shared between the worker and the backend to prevent unauthorized access.
# Generate a long, random string for this.
WORKER_SECRET=

# -- Logging Settings --
# The level of detail for logs. Can be DEBUG, INFO, WARNING, or ERROR.
# Use DEBUG for development and ERROR for production.
LOG_LEVEL=DEBUG
