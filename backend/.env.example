# This is an example environment file.
# Copy this file to .env and fill in the actual values.

# A strong, randomly generated secret key that must match the one used by the Cloudflare Worker.
# This is used to authenticate requests between the worker and the backend.
WORKER_SECRET=your_strong_random_secret_here