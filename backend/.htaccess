#
# 文件名: .htaccess
# 路径: backend/ (项目根目录)
# 版本: Final Corrected
#

<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>

# 核心安全配置：阻止对 .env 文件的直接 Web 访问请求
# 这是一个备用安全层，因为 .env 文件本身就不在 Web 根目录下
<FilesMatch "\.(env)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>