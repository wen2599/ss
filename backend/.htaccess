#
# 文件名: .htaccess
# 路径: backend/.htaccess
#
<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>

# 这是一个备用安全层，防止 .env 文件被意外放入此目录
<FilesMatch "\.(env)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>