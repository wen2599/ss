#
# 文件名: .htaccess
# 路径: 项目根目录
# 版本: Final Corrected
#

# 核心安全配置：阻止通过浏览器直接访问 .env 文件
# 由于 .env 在上一级，这个规则主要是为了防止意外地把它放在根目录。
<FilesMatch "\.(env)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# 保护项目内部的 config.php 和 install.php 不被外部访问
<FilesMatch "^(config\.php|install\.php)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>