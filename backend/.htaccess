# ==========================================================
# .htaccess for Serv00 Hosting Environment
# ==========================================================

# ----------------------------------------------------------
# Section 1: Disable mod_security for Telegram Webhook
# This is the critical fix for the 403 Forbidden error.
# It tells the server's firewall to not block requests
# that are sent to "webhook.php".
# ----------------------------------------------------------
<Files "webhook.php">
  <IfModule mod_security.c>
    SecFilterEngine Off
    SecFilterScanPOST Off
  </IfModule>
</Files>

# ----------------------------------------------------------
# Section 2: No Rewrite Rules
# DO NOT ADD ANY "RewriteEngine On" or "RewriteRule" here.
# Based on our tests, they cause 502 errors on your server.
# URL mapping is handled by the Cloudflare Worker (_worker.js).
# ----------------------------------------------------------

# End of .htaccess file