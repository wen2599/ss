# Please fill in your database credentials below.
# Do not use quotes around the values.

DB_HOST=your_database_host
DB_USER=your_database_username
DB_PASS=your_database_password
DB_NAME=your_database_name

# You also need to set the secret for the Cloudflare Worker.
# This must match the secret you set in your Cloudflare Worker settings.
EMAIL_HANDLER_SECRET=your_secret_token_here
