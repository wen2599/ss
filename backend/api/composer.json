{
    "name": "your-namespace/email-processor-backend",
    "description": "Backend for receiving and serving emails, processed by a Cloudflare Worker.",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Your Name",
            "email": "your.email@example.com"
        }
    ],
    "require": {
        "php": ">=8.1",
        "vlucas/phpdotenv": "^5.5",
        "illuminate/database": "^10.0",
        "irazasyed/telegram-bot-sdk": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "process-timeout": 0
    },
    "scripts": {
        "start": "php -S localhost:8888 -t .",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
