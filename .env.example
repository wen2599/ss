# --------------------------------------------------------------------------
# 数据库配置
# 这些变量用于连接到您的MySQL数据库。
# --------------------------------------------------------------------------
DB_HOST=127.0.0.1
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_PORT=3306

# --------------------------------------------------------------------------
# Telegram Bot 配置
# 这些变量用于您的Telegram Bot 与 Telegram API 交互。
# --------------------------------------------------------------------------
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHANNEL_ID=your_telegram_channel_id # 用于接收彩票结果的Telegram频道ID
TELEGRAM_ADMIN_ID=your_telegram_admin_id # 用于接收错误通知的Telegram管理员ID

# --------------------------------------------------------------------------
# CORS (跨域资源共享) 配置
# 这个变量定义了允许访问后端API的前端域名。
# 如果有多个域名，请用逗号分隔。
# --------------------------------------------------------------------------
ALLOWED_ORIGINS=https://ss.wenxiuxiu.eu.org,http://localhost:5173

# --------------------------------------------------------------------------
# 应用调试模式
# 设置为 'true' 以在错误响应中显示详细的错误信息。
# 在生产环境中，应始终设置为 'false'。
# --------------------------------------------------------------------------
APP_DEBUG=false
